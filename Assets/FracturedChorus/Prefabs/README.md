# Prefabs

Extract from runtime-built objects when UI/combat layout stabilizes.

| Subfolder | Use |
|-----------|-----|
| `Combat/` | Unit view, grid cell marker |
| `UI/` | Beat segment, skill button, timeline bar root |

Prototype v1 builds UI at runtime in `CombatPrototypeBootstrap` — prefabs optional until art pass.
